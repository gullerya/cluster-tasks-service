language: java
jdk: oraclejdk8
install: true
services:
  - postgresql

# TEST

# SPOTBUGS
script:
  - mvn test -T8 -Ddb.config.location=environment -Ddb.type=POSTGRESQL -Ddb.url=jdbc:postgresql://localhost:5432/cts_tests_schema -Ddb.username=postgres -Pcreate-tests-schema,drop-tests-schema,postgresql,jacoco-coverage
  - mvn spotbugs:check -T8